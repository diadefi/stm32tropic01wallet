#include "libtropic_common.h"

/*
 * TEMPORARY STUB ONLY.
 *
 * This lets lt_init()/lt_deinit() link for simple L2 tests like Get_Info.
 * It is NOT valid for secure channel, PIN, encrypted storage, authentication,
 * or any real wallet security feature.
 */

lt_ret_t lt_crypto_ctx_init(void *ctx)
{
    (void)ctx;
    return LT_OK;
}

lt_ret_t lt_crypto_ctx_deinit(void *ctx)
{
    (void)ctx;
    return LT_OK;
}
