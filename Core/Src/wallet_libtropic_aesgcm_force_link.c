/*
 * Disabled for B1 recovery.
 *
 * B1 VERSION / SEINFO / SEKEYINFO does not use real L3 crypto commands.
 * The old no-crypto stub is restored separately to satisfy lt_init()
 * crypto context symbols.
 */
